<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>UNO Online</title>
    <link rel="icon" type="image/svg+xml" href="/public/favicon.svg" />
    <link href="https://fonts.googleapis.com/css2?family=Fredoka+One:wght@400&family=Roboto:wght@500;700&display=swap" rel="stylesheet">
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: 'Roboto', sans-serif;
        height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background: 
          radial-gradient(circle at center, #ff5100 0%, transparent 60%),
          linear-gradient(135deg, #ff9800 0%, #ff6e07 50%, #ff9800 100%);
        background-color: #ff8f00;
        color: white;
        text-align: center;
        padding: 20px;
        min-height: 100dvh;
      }

      .welcome-container {
        width: 100%;
        max-width: 500px;
        text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
      }

      .logo img {
        width: 100%;
        max-width: 400px;
        height: auto;
        margin-bottom: 30px;
        border-radius: 16px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.4);
        border: 4px solid rgba(255, 255, 255, 0.3);
      }

      h1 {
        font-family: 'Fredoka One', cursive;
        font-size: 4.5rem;
        margin-bottom: 50px;
        text-shadow: 0 4px 15px rgba(0, 0, 0, 0.5);
        color: #fff;
        letter-spacing: 2px;
      }

      .button-group {
        display: flex;
        flex-direction: column;
        gap: 20px;
        align-items: center;
      }

      .btn {
        display: block;
        width: 80%;
        max-width: 300px;
        padding: 18px 0;
        font-size: 1.5rem;
        font-weight: 700;
        text-decoration: none;
        color: white;
        border-radius: 14px;
        transition: all 0.3s ease;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
      }

      .btn-primary {
        background: #d32f2f;
      }

      .btn-primary:hover {
        background: #f44336;
        transform: translateY(-4px);
        box-shadow: 0 12px 25px rgba(211, 47, 47, 0.4);
      }

      .btn-secondary {
        background: rgba(255, 255, 255, 0.25);
        backdrop-filter: blur(8px);
        border: 2px solid rgba(255, 255, 255, 0.4);
      }

      .btn-secondary:hover {
        background: rgba(255, 255, 255, 0.35);
        transform: translateY(-4px);
      }

      @media (min-width: 600px) {
        h1 { font-size: 5.5rem; }
        .button-group { flex-direction: row; justify-content: center; gap: 30px; }
        .btn { width: auto; padding: 18px 50px; }
      }

      @media (max-width: 400px) {
        h1 { font-size: 3.4rem; margin-bottom: 40px; }
        .btn { font-size: 1.3rem; }
        .logo img { border-radius: 12px; }
      }
    </style>
  </head>
  <body>
    <div class="welcome-container">
      <div class="logo">
        <img src="https://cdn2.unrealengine.com/Diesel%2Fproductv2%2Funo%2Fhome%2FGameName_Store_Landscape_2560x1440-2560x1440-5195e8a3e06d672f97a1ee49ecea59027c14cae4.jpg" alt="UNO Cards Logo">
      </div>
      <h1>UNO Online</h1>
      <div class="button-group">
        <a href="/auth/login" class="btn btn-secondary">LOGIN</a>
        <a href="/auth/signup" class="btn btn-primary">SIGN UP</a>
      </div>
    </div>
  </body>
</html>