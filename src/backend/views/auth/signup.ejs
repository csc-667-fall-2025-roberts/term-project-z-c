<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>UNO Online - Sign Up</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="min-h-screen flex items-center justify-center bg-gradient-to-br from-[#1a1a2e] to-[#16213e] font-sans p-5">
    <div class="w-full max-w-md bg-white rounded-3xl shadow-2xl p-12">
      <div class="text-center mb-8">
        <div class="text-6xl mb-4">🎴</div>
        <h1 class="text-4xl font-black text-gray-800 uppercase tracking-wider">
          UNO Online
        </h1>
        <p class="text-gray-500 mt-2 font-semibold">Create Your Account</p>
      </div>

      <form method="post" action="/auth/signup" class="space-y-5">
        <% if (locals.error) { %>
          <p class="bg-red-50 border-2 border-red-400 text-red-700 px-4 py-3 rounded-xl text-sm font-medium">
            <%= locals.error %>
          </p>
        <% } %>

        <div class="flex flex-col gap-2">
          <label for="username" class="text-sm font-bold text-gray-700 uppercase tracking-wide">Username</label>
          <input id="username" type="text" name="username" placeholder="Choose a username"
            class="px-4 py-3 border-2 border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-yellow-500 focus:border-yellow-500 transition" />
        </div>

        <div class="flex flex-col gap-2">
          <label for="email" class="text-sm font-bold text-gray-700 uppercase tracking-wide">Email</label>
          <input id="email" type="email" name="email" placeholder="Enter your email"
            class="px-4 py-3 border-2 border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-yellow-500 focus:border-yellow-500 transition" />
        </div>

        <div class="flex flex-col gap-2">
          <label for="password" class="text-sm font-bold text-gray-700 uppercase tracking-wide">Password</label>
          <input id="password" type="password" name="password" placeholder="Create a password"
            class="px-4 py-3 border-2 border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-yellow-500 focus:border-yellow-500 transition" />
        </div>

        <div class="flex flex-col gap-4 mt-6">
          <button type="submit" class="w-full bg-gradient-to-r from-yellow-400 to-yellow-500 text-black font-bold py-4 rounded-full shadow-lg hover:shadow-xl hover:from-yellow-500 hover:to-yellow-600 transition transform hover:-translate-y-1 uppercase tracking-wider text-lg border-2 border-white">
            Sign Up
          </button>
          <a href="/auth/login" class="text-center text-gray-800 hover:text-yellow-500 text-sm font-semibold transition">Already have an account? Login</a>
        </div>
      </form>
    </div>
  </body>
</html>